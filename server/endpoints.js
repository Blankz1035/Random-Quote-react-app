const ENDPOINTS = {
    BLOCK_QUOTES: "quotes", // - Generate a JSON array of 50 random quotes on each request
    TODAY_QUOTE: "today", // - Generate the quote of the day on each request
    RANDOM_QUOTE: "random", //- Generate a random quote on each request
    IMAGE_QUOTE: "image" //- Generate a random inspirational image on each request.
}

module.exports = ENDPOINTS